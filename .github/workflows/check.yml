name: Check

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -D warnings

jobs:
  check:
    name: fmt + clippy + build + test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry, git, and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-host-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-host-

      # Mesa software rasteriser so the wgpu adapter request succeeds on
      # ubuntu-latest runners (no GPU available). Used by the `smoke` and
      # `picking` integration tests. fontconfig is the system font-discovery
      # library parley/fontique links against on Linux.
      - name: Install Mesa Vulkan drivers and fontconfig
        run: |
          sudo apt-get update
          sudo apt-get install -y mesa-vulkan-drivers libvulkan1 vulkan-tools libfontconfig1-dev

      - name: cargo fmt --check
        run: cargo fmt --all -- --check

      - name: cargo clippy --all-features
        run: cargo clippy --all-features --all-targets -- -D warnings

      - name: cargo build --no-default-features
        run: cargo build --no-default-features

      # Each raster writer must stand on its own: they share a module, so one
      # leaning on another's gated code would still pass every combination
      # above. The `--all-features` runs never build a feature in isolation.
      - name: cargo check, one writer feature at a time
        run: |
          for feature in png jpeg tiff webp; do
            cargo check --no-default-features --features "$feature"
          done

      # `image-url` adds a read path behind a *second* gate: reading a URL
      # needs both the HTTP client and a codec to decode what came back, and
      # the feature is useless — and unbuilt — without one.
      - name: cargo check, the URL reader with and without a codec
        run: |
          cargo check --no-default-features --features image-url
          cargo check --no-default-features --features image-url,png --all-targets

      # Image embedding gates on `png` *and* on a document direction, so the
      # two axes have to be crossed. A codec-only helper reachable from the
      # read half, or a collector reachable from the write half, passes every
      # single-feature build above and fails exactly one of these.
      - name: cargo check, document directions crossed with the image codec
        run: |
          for feature in document-write,png document-read,png document,png; do
            cargo check --no-default-features --features "$feature" --all-targets
          done

      # The SVG backend embeds a raster image by re-encoding it, so that one
      # path gates on `png` while the rest of the backend gates on nothing.
      # Both halves have to stand alone, for the same reason the document
      # jobs cross their two axes.
      - name: cargo check, the SVG backend with and without an image codec
        run: |
          cargo check --no-default-features --features svg --all-targets
          cargo check --no-default-features --features svg,png --all-targets

      # The PDF backend needs no codec for a raster image — PDF takes raw
      # samples — but it does reach for `png` to decode a bitmap color
      # glyph, which is how most emoji ship. Same two-halves reasoning as
      # the SVG job above.
      - name: cargo check, the PDF backend with and without an image codec
        run: |
          cargo check --no-default-features --features pdf --all-targets
          cargo check --no-default-features --features pdf,png --all-targets

      # The sparse-strips backend must stand on its own: `--all-features`
      # builds it beside the compute-shader one, which would hide anything it
      # reaches for through the other backend's gates.
      - name: cargo clippy, the sparse-strips backend alone
        run: |
          cargo clippy --no-default-features --features vello-hybrid,png \
            --all-targets -- -D warnings

      # Presentation must work with either backend behind it, and the
      # hybrid-only combination is the one that would break if the window host
      # reached for the other backend's gates.
      - name: cargo clippy, presentation on each backend
        run: |
          for backend in vello vello-hybrid; do
            cargo clippy --no-default-features \
              --features "window,$backend,png" --all-targets -- -D warnings
          done

      - name: cargo test --all-features
        run: cargo test --all-features

  # `vello`, `vello_hybrid` and `parley` all declare 1.88, so that is the floor
  # for any build that includes them, whatever `rust-version` says. The jobs above all run on
  # stable and would not catch a newer language or std feature creeping in.
  msrv:
    name: MSRV (1.88, --all-features)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.88
        uses: dtolnay/rust-toolchain@1.88

      - name: Cache cargo registry, git, and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-msrv-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-msrv-

      - name: Install fontconfig
        run: |
          sudo apt-get update
          sudo apt-get install -y libfontconfig1-dev

      - name: cargo check --all-features
        run: cargo check --all-features

  # The renderer-free document writer is what an R package vendoring this crate
  # for CRAN builds, and CRAN's toolchain is older than the 1.88 the job above
  # pins. `--ignore-rust-version` is needed because the `parley` family declares
  # 1.88 and is compiled here even though the renderer is not — the declaration
  # is what blocks the build, not the code, which compiles on 1.86. Dropping the
  # flag is what making `parley` optional would buy.
  msrv-document:
    name: MSRV (1.86, renderer-free document writer)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust 1.86
        uses: dtolnay/rust-toolchain@1.86

      - name: Cache cargo registry, git, and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-msrv-document-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-msrv-document-

      - name: Install fontconfig
        run: |
          sudo apt-get update
          sudo apt-get install -y libfontconfig1-dev

      # One direction at a time, for the reason `src/document/CLAUDE.md` gives:
      # a helper reachable from only one half still passes both other builds.
      - name: cargo check, one document feature at a time
        run: |
          for feature in document-write document-read document; do
            cargo check --no-default-features --features "$feature" \
              --all-targets --ignore-rust-version
          done

      # The point of a renderer-free vector backend: a document in and an SVG
      # out, on the oldest rustc this crate supports. `vello` rules that out,
      # so this is the only configuration that can carry the claim — and the
      # only thing keeping it true.
      - name: cargo check, document in / SVG out on the oldest rustc
        run: |
          cargo check --no-default-features --features document-read,svg \
            --all-targets --ignore-rust-version

      # And the same claim for the fixed vector backend, which adds only
      # `flate2` over what `svg` needs.
      - name: cargo check, document in / PDF out on the oldest rustc
        run: |
          cargo check --no-default-features --features document-read,pdf \
            --all-targets --ignore-rust-version

  # wasm32 is a supported target, so it gets its own job: it shares no compiled
  # artifacts with the host build and needs none of the system libraries above
  # (fontconfig and Vulkan are both gated to native targets). Catches
  # GL-backend and dependency regressions that the host build misses.
  wasm:
    name: wasm32 check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo registry, git, and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: ${{ runner.os }}-cargo-wasm-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-wasm-

      - name: cargo check --target wasm32-unknown-unknown
        run: cargo check --target wasm32-unknown-unknown

      # The raster encoders are pure Rust and expected to build for wasm; the
      # default-feature check above would not notice one that isn't. Named
      # explicitly rather than `--all-features` because `google-fonts` pulls
      # `ureq`, which has no wasm target.
      - name: cargo check --target wasm32-unknown-unknown --features jpeg,tiff,webp
        run: cargo check --target wasm32-unknown-unknown --features jpeg,tiff,webp

      # Downloading an SVG of the plot is an obvious thing for a page to
      # offer, so the backend has to pull nothing that is not wasm-clean.
      - name: cargo check --target wasm32-unknown-unknown, SVG out
        run: |
          cargo check --target wasm32-unknown-unknown --no-default-features \
            --features svg,document-read

      # A PDF download is the other obvious thing for a page to offer, and
      # `flate2` has to resolve to its pure-Rust backend to get there.
      - name: cargo check --target wasm32-unknown-unknown, PDF out
        run: |
          cargo check --target wasm32-unknown-unknown --no-default-features \
            --features pdf,document-read

      # The render client's exact configuration: the canvas host plus a
      # read-only document reader, and nothing else. Checked on its own
      # because it is the one combination a website actually ships, and
      # because `canvas` compiles a host no other target has.
      # The client's default is WebGL2, so that is the configuration checked
      # first; `wgpu-backend` is the alternative and is checked after it.
      - name: cargo clippy, the wasm render client (default, WebGL2)
        working-directory: crates/hephaestus-wasm
        run: cargo clippy --target wasm32-unknown-unknown -- -D warnings

      - name: cargo clippy, the wasm render client on wgpu
        working-directory: crates/hephaestus-wasm
        run: |
          cargo clippy --target wasm32-unknown-unknown --no-default-features \
            --features webfonts,wgpu-backend -- -D warnings

      # The SVG client, which has no rasteriser at all: no wgpu, no
      # vello_hybrid, no GL bindings. Checked on its own because it is the
      # other combination a website actually ships, and because it is the one
      # that breaks if the SVG backend or the document reader grows a
      # dependency that is not wasm-clean.
      - name: cargo clippy, the wasm SVG client
        working-directory: crates/hephaestus-svg-wasm
        run: cargo clippy --target wasm32-unknown-unknown -- -D warnings

      # Without `webfonts`, which is the configuration that refuses WOFF by
      # name rather than letting it reach the shaper and register nothing.
      - name: cargo clippy, the wasm SVG client without webfonts
        working-directory: crates/hephaestus-svg-wasm
        run: |
          cargo clippy --target wasm32-unknown-unknown --no-default-features \
            -- -D warnings

      # `window` on wasm has no caller — `run` is desktop-only — but the
      # module claims to compile there, and nothing else proves it.
      - name: cargo check --target wasm32-unknown-unknown --features window
        run: cargo check --target wasm32-unknown-unknown --features window

      # The sparse-strips backend over wgpu on wasm: still WebGPU, because
      # wgpu's own `webgl` feature is not compiled here (it costs +1.2 MB, being
      # shader translation). Going below WebGPU is the `webgl` feature's job,
      # checked separately below.
      - name: cargo clippy --target wasm32-unknown-unknown, sparse strips
        run: |
          cargo clippy --target wasm32-unknown-unknown --no-default-features \
            --features vello-hybrid,document-read -- -D warnings

      # What the render client would ship on sparse strips. Still WebGPU: see
      # the note above.
      # The WebGL2 build: no wgpu in the tree at all, so it is the one that
      # breaks if the scene layer reaches for a wgpu type.
      - name: cargo clippy --target wasm32-unknown-unknown, webgl
        run: |
          cargo clippy --target wasm32-unknown-unknown --no-default-features \
            --features webgl,document-read -- -D warnings

      - name: cargo clippy --target wasm32-unknown-unknown, canvas on each backend
        run: |
          for backend in vello vello-hybrid; do
            cargo clippy --target wasm32-unknown-unknown --no-default-features \
              --features "canvas,$backend,document-read" -- -D warnings
          done

  # The wasm bundle a page loads. Its own job because `crates/hephaestus-wasm`
  # is a separate workspace with its own lock file and release profile, so it
  # shares no artifacts with the checks above.
  client:
    name: wasm render client (wasm-pack)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo registry, git, and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            crates/hephaestus-wasm/target
          key: ${{ runner.os }}-cargo-wasm-${{ hashFiles('crates/hephaestus-wasm/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-wasm-

      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.4.0

      # wasm-pack uses a `wasm-opt` from PATH and downloads its own copy only
      # when it finds none. That download failing is not an error to it — it
      # warns and emits an unoptimized binary — and wasm-opt is worth about a
      # quarter of the bundle, so installing it here keeps a network hiccup
      # from silently shipping 3.9 MB in place of 2.9 MB.
      - name: Install wasm-opt (binaryen)
        env:
          BINARYEN: version_132
          BINARYEN_SHA256: 195ddc94f9bc89f45abdabb0b9eea86023d727ba90eac8b35b80f2544fc30572
        run: |
          curl -fsSL -o binaryen.tar.gz \
            "https://github.com/WebAssembly/binaryen/releases/download/$BINARYEN/binaryen-$BINARYEN-x86_64-linux.tar.gz"
          echo "$BINARYEN_SHA256  binaryen.tar.gz" | sha256sum -c -
          tar -xzf binaryen.tar.gz
          "$PWD/binaryen-$BINARYEN/bin/wasm-opt" --version
          echo "$PWD/binaryen-$BINARYEN/bin" >> "$GITHUB_PATH"

      - name: assemble the package
        working-directory: crates/hephaestus-wasm
        run: ./build.sh

      # Loads the assembled package the way a consumer meets it and checks the
      # manifest against what is on disk: a renamed export, a file missing
      # from `files[]`, a version out of step with the crate, or a wrapper
      # importing the glue by the wrong path all fail here rather than after
      # publishing.
      - name: verify the package
        working-directory: crates/hephaestus-wasm
        run: node verify-dist.mjs

      # `npm pack` is the last word on what would actually ship.
      - name: report the published tarball
        working-directory: crates/hephaestus-wasm/dist
        run: |
          npm pack --dry-run --json > /tmp/pack.json
          node -e "
            const p = require('/tmp/pack.json')[0];
            const names = p.files.map((f) => f.path).sort().join(', ');
            console.log('tarball: ' + p.filename);
            console.log('files:   ' + names);
            console.log('size:    ' + p.size + ' packed / ' + p.unpackedSize + ' unpacked');
          " | tee -a "$GITHUB_STEP_SUMMARY"
          wasm=hephaestus_wasm_bg.wasm
          echo "wasm: $(stat -c%s $wasm) raw; $(brotli -q 11 -c $wasm | wc -c) brotli" \
            | tee -a "$GITHUB_STEP_SUMMARY"

  # The other wasm bundle a page can load: same documents, no rasteriser.
  # Its own job for the same reason `client` is one — `crates/hephaestus-svg-wasm`
  # is a separate workspace with its own lock file and release profile.
  client-svg:
    name: wasm SVG client (wasm-pack)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown

      - name: Cache cargo registry, git, and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            crates/hephaestus-svg-wasm/target
          key: ${{ runner.os }}-cargo-svg-wasm-${{ hashFiles('crates/hephaestus-svg-wasm/Cargo.toml') }}
          restore-keys: |
            ${{ runner.os }}-cargo-svg-wasm-

      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.4.0

      # Same reasoning as the `client` job: wasm-pack warns rather than fails
      # when its own wasm-opt download does not arrive, and silently ships an
      # unoptimized binary.
      - name: Install wasm-opt (binaryen)
        env:
          BINARYEN: version_132
          BINARYEN_SHA256: 195ddc94f9bc89f45abdabb0b9eea86023d727ba90eac8b35b80f2544fc30572
        run: |
          curl -fsSL -o binaryen.tar.gz \
            "https://github.com/WebAssembly/binaryen/releases/download/$BINARYEN/binaryen-$BINARYEN-x86_64-linux.tar.gz"
          echo "$BINARYEN_SHA256  binaryen.tar.gz" | sha256sum -c -
          tar -xzf binaryen.tar.gz
          "$PWD/binaryen-$BINARYEN/bin/wasm-opt" --version
          echo "$PWD/binaryen-$BINARYEN/bin" >> "$GITHUB_PATH"

      # The fixture `verify-dist.mjs` renders end to end. Unlike the canvas
      # client, this one can draw with no DOM and no GPU, so a document that
      # fails to render fails the build here rather than on a page.
      - name: build the document fixture
        run: |
          cargo run --example document_save --features document-write
          cp examples/document.hep crates/hephaestus-svg-wasm/www/

      - name: assemble the package
        working-directory: crates/hephaestus-svg-wasm
        run: ./build.sh

      - name: verify the package
        working-directory: crates/hephaestus-svg-wasm
        run: node verify-dist.mjs

      # The half `verify-dist.mjs` cannot reach: ResizeObserver, the markup
      # swap and `elementFromPoint` picking. Ubuntu runners ship Chrome, so
      # this costs an env var rather than an install step.
      - name: browser smoke test
        working-directory: crates/hephaestus-svg-wasm
        env:
          HEPHAESTUS_CHROME: /usr/bin/google-chrome
        run: node bench/smoke.mjs

      # `npm pack` is the last word on what would actually ship.
      - name: report the published tarball
        working-directory: crates/hephaestus-svg-wasm/dist
        run: |
          npm pack --dry-run --json > /tmp/pack-svg.json
          node -e "
            const p = require('/tmp/pack-svg.json')[0];
            const names = p.files.map((f) => f.path).sort().join(', ');
            console.log('tarball: ' + p.filename);
            console.log('files:   ' + names);
            console.log('size:    ' + p.size + ' packed / ' + p.unpackedSize + ' unpacked');
          " | tee -a "$GITHUB_STEP_SUMMARY"
          wasm=hephaestus_svg_wasm_bg.wasm
          echo "wasm: $(stat -c%s $wasm) raw; $(brotli -q 11 -c $wasm | wc -c) brotli" \
            | tee -a "$GITHUB_STEP_SUMMARY"
