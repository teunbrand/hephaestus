/target
examples/*.png
# Example output, same as the PNGs: regenerated by running the example,
# and 30 kB of text that would churn on every change if tracked.
examples/*.svg
examples/*.pdf
examples/*.jpg
examples/*.tiff
examples/*.webp
# Written by examples/document_save.rs, read by document_load.
examples/*.hep
tests/*.png
.claude/
PLAN.md
.vscode/
READING_GUIDE.md
